{
  "name": "website",
  "version": "1.0.0",
  "description": "",
  "main": "app.jsx",
  "scripts": {
    "build:clean": "rm -r ./dist",
    "build:prod": "esbuild app.jsx --bundle --minify --sourcemap --target=chrome58,firefox57,safari11,edge16",
    "build:dev": "node ./scripts/build/development.js",
    "build": "npm run build:clean && npm run build:dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/react": "^17.0.2",
    "@types/react-dom": "^17.0.1",
    "esbuild": "^0.8.49",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
